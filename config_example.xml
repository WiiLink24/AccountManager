<Config>
    <!-- The address which HTTP will bind to -->
    <address>127.0.0.1:9011</address>

    <!-- Credentials -->
    <username>username</username>
    <password>password</password>

    <!-- Database information-->
    <dominosDatabaseAddress>127.0.0.1:5432</dominosDatabaseAddress>
    <dominosDatabaseName>demae</dominosDatabaseName>

    <dominosDatabaseUsername>dominos</dominosDatabaseUsername>
    <dominosDatabasePassword>thedom</dominosDatabasePassword>

    <!-- OIDC credentials/data -->
    <oidc>
        <clientID></clientID>
        <clientSecret></clientSecret>
        <redirectURL>http://localhost:9011/authorize</redirectURL>
        <scopes>openid profile email goauthentik.io/api wii</scopes>
        <provider></provider>
        <logoutURL></logoutURL>
    </oidc>
</Config>